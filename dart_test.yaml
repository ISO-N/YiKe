# 文件用途：Dart/Flutter 测试全局配置（限制单测超时与并发，避免卡住导致的无限等待）。
# 作者：Codex
# 创建日期：2026-02-25
#
# 说明：
# - 该配置会被 `flutter test` / `dart test` 读取。
# - 通过限制每个测试用例的最长执行时间，降低 `pumpAndSettle` / 异步轮询等场景的挂死风险。
# - 通过限制并发，避免数据库/静态缓存等全局状态在并行测试中相互干扰。
timeout: 10s
concurrency: 1
