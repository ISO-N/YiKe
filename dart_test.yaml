# 文件用途：Dart/Flutter 测试全局配置（限制单测超时，避免循环等待导致挂死）。
# 作者：Codex
# 创建日期：2026-02-25

# 说明：
# - 该配置会被 `flutter test` / `dart test` 读取。
# - 为了避免出现 `pumpAndSettle` / 异步轮询等场景的无限等待，统一限制每个测试用例的最长执行时间。
timeout: 10s

